import { register } from '../../../utils/widget-props';

export const registerProps = () => {
    register(
        'wm-dialogactions',
        new Map(
            [
            ]
        )
    );
};
